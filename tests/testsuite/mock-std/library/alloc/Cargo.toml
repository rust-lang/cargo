[package]
name = "alloc"
version = "0.1.0"
authors = ["Alex Crichton <alex@alexcrichton.com>"]
edition = "2018"

[dependencies]
core = { path = "../core" }
registry-dep-using-core = { version = "*", features = ['mockbuild'] }
